# Go
/bin/
promptvoice-cli
*.test
*.out

# Vendored native deps (built locally via scripts/setup-whisper.sh)
/third_party/

# Whisper / LLM model files (large binaries — download at setup time)
/models/
*.bin
*.gguf
*.ggml

# Audio debug dumps
*.wav

# Node / frontend
node_modules/

# Wails build output + generated bindings (regenerated by `wails build`)
desktop/build/bin/
desktop/frontend/wailsjs/

# Distributable installers (repo-root only — desktop/frontend/dist/ is actual
# frontend source, not build output, and must stay tracked)
/dist/

# OS
.DS_Store
